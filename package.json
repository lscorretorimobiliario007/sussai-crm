{
  "name": "sistema-imobiliaria",
  "private": true,
  "version": "1.0.0-rc1",
  "description": "SUSSAI CRM + Portal Top Conceição",
  "scripts": {
    "check": "npm run check --prefix backend && npm run lint --prefix frontend && npm run lint --prefix top-conceicao-site",
    "build": "npm run build --prefix backend && npm run build --prefix frontend && npm run build --prefix top-conceicao-site",
    "validate": "npm run check && npm run build"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.1",
    "@hookform/resolvers": "^5.4.0",
    "@mui/icons-material": "^9.2.0",
    "@mui/material": "^9.2.0",
    "axios": "^1.18.1",
    "helmet": "^8.3.0",
    "react-hook-form": "^7.81.0",
    "react-router-dom": "^7.18.1",
    "zod": "^4.4.3"
  }
}
